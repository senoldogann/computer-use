# Copy to .env (gitignored) or to ~/.computeruse/env, then fill in your key.
# Resolution order: the process environment, then ./.env, then ~/.computeruse/env.
OPENAI_API_KEY=sk-...

# Optional: override the default model for this checkout.
# OPENAI_MODEL=gpt-5.6-terra
